# App build output only — do not ignore vendor package dists (needed on Vercel).
/dist
node_modules
.netlify
.wrangler
.vercel
.env*
!.env.example
.dev.vars
# Keep .pracht/app-graph.json committed — it is the `pracht plan` snapshot.
